spring:
  datasource:
    url: jdbc:postgresql://sosomoim.ca1avol0icet.ap-northeast-2.rds.amazonaws.com:5432/postgres
    username: root
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none # in production -> none
    properties:
      hibernate:
        show_sql: false
        format_sql: false
        use_sql_comments: false

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
